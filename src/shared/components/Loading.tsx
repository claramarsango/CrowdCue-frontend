import { LoadingSpinner } from './LoadingStyled';

const Loading = () => {
  return (
    <LoadingSpinner>
      <div className="spinner"></div>
    </LoadingSpinner>
  );
};

export default Loading;
